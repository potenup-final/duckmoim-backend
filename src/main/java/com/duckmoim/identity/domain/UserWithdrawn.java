package com.duckmoim.identity.domain;

/**
 * 회원이 탈퇴했다 (AU-11).
 *
 * <p><b>Identity 가 Notification 을 모르기 위해 있다.</b> 도메인-모델링.md 「2. 바운디드 컨텍스트」가 의존을 {@code Notification
 * ──▶ Identity} 한 방향으로 정했다. 탈퇴가 푸시 구독을 지워야 하는데 {@code UserService} 가 구독 저장소를 부르면 그 화살표가 뒤집힌다.
 *
 * <p>{@code CompanionPostOpened} 가 같은 이유로 먼저 있고, <b>구독 규약도 그 기록에 적혀 있다.</b> 이 기록은 그 규약의 ①이다 — 탈퇴와
 * <b>같은 트랜잭션에서</b> 처리되어야 한다. 탈퇴가 롤백되면 구독도 남아야 하고, 탈퇴가 커밋되면 구독은 반드시 없어야 한다.
 *
 * <p><b>구독자가 둘이고 규약이 갈린다</b> (STAR-148). 푸시 구독 지우기는 위의 ①이고, 채팅의 열린 스트림 끊기({@code
 * ChatStreamRevocationService})는 ② — 밖으로 나가는 호출이라 탈퇴가 커밋된 뒤에 돈다.
 *
 * <p><b>왜 반드시인가.</b> 탈퇴는 토큰만 끊고 행은 남기는 소프트 삭제라 (진짜 파기는 AD-05) 그 사람의 옛 댓글에 답글이 달리면 알림이 그대로 발행된다. 인앱
 * 알림은 로그인이 막혀 아무도 못 보지만 <b>푸시는 로그인 없이 기기에 직접 닿는다</b> — 탈퇴한 사람의 폰에 알림이 뜬다.
 *
 * <p><b>회원번호만 담는다.</b> 애그리게이트를 실어 보내면 듣는 쪽이 그것을 붙들 수 있게 되어 ID 참조 규칙이 무너진다 (도메인-모델링.md 「3.2」).
 */
public record UserWithdrawn(Long userId) {}

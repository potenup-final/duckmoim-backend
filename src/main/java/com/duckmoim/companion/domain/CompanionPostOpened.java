package com.duckmoim.companion.domain;

/**
 * 모집글이 열렸다 (PO-01).
 *
 * <p><b>Companion 이 Chat 을 모르기 위해 있다.</b> 도메인-모델링.md 「2. 바운디드 컨텍스트」가 의존을 {@code Chat ──▶ Companion}
 * 한 방향으로 정했고 <i>"B는 A를 모른다"</i> 고 읽는 법까지 적었다. CH-01 이 「모집글을 쓰면 방이 함께 생긴다」이므로 모집글 작성이 방을 만들어야 하는데,
 * {@code CompanionPostCommandService} 가 채팅 서비스를 부르면 그 화살표가 뒤집힌다.
 *
 * <p><b>그래서 이 기록은 「무엇을 해라」가 아니라 「무엇이 일어났다」다.</b> 방을 만들라는 지시가 아니라 글이 열렸다는 사실이고, 그것을 듣고 무엇을 할지는 듣는 쪽이
 * 정한다. 구독자가 늘어도 이 기록에 손댈 일이 없어야 한다.
 *
 * <p><b>domain 에 둔다.</b> 발행하는 쪽과 듣는 쪽이 모두 service 인데 그 둘 사이에 두면 한쪽 service 가 다른 service 를 알아야 한다.
 * 어느 레이어에서나 볼 수 있는 자리는 domain 이고, 프레임워크에 묶이지 않은 record 라 아키텍처 규칙에도 걸리지 않는다.
 *
 * <p><b>모집글을 담지 않고 번호만 담는다.</b> 애그리게이트를 실어 보내면 듣는 쪽이 그것을 붙들 수 있게 되어 ID 참조 규칙이 무너진다 (도메인-모델링.md 「3.2
 * 애그리게이트 간 참조 규칙」).
 *
 * <h2>구독 규약</h2>
 *
 * <p><b>{@code publishEvent} 는 큐가 아니다.</b> 그 자리에서 리스너를 동기 호출하므로 {@code @EventListener} 로 붙은 리스너는
 * <b>발행한 트랜잭션에 그대로 올라탄다</b> — 리스너가 던지면 모집글 INSERT 까지 롤백된다. 지금 구독자는 방 개설 하나이고 그것은 의도한 동작이지만 ({@code
 * ChatRoomOpenService}), 아무것도 적어 두지 않으면 <b>리스너 하나를 위한 결정이 앞으로 붙는 모든 리스너의 기본값으로 굳는다.</b> 붙일 자리를 찾는
 * 사람이 먼저 읽는 곳이 이 기록이라 규약을 여기 둔다.
 *
 * <p>기준은 하나다 — <b>실패했을 때 무엇이 남아야 하는가.</b>
 *
 * <p><b>① 커밋과 원자적으로 남아야 하는 기록</b> — 같은 트랜잭션에서 돈다. {@code @EventListener} +
 * {@code @Transactional(MANDATORY)}. 지금은 방 개설 하나다.
 *
 * <p><b>② 밖으로 나가는 호출</b> (푸시 · 문자 · HTTP) — 같은 트랜잭션에 두지 않는다.
 * {@code @TransactionalEventListener(AFTER_COMMIT)} + {@code REQUIRES_NEW}. 이유는 명백하다. 푸시 API 타임아웃
 * 하나가 사용자의 글을 지운다.
 *
 * <p><b>알림에서 ②의 대상은 발송이고 기록이 아니다.</b> 여기를 헷갈리면 알림을 통째로 {@code AFTER_COMMIT} 으로 밀게 되는데, I-25 가
 * <i>"알림 발송 실패가 도메인 트랜잭션을 롤백시키지 않는다"</i>의 검증 위치를 <b>아웃박스 발행</b>으로, 이중 방어를 <b>"워커가 트랜잭션 밖에서
 * 발송한다"</b>로 정했다 (도메인-모델링.md 「5. 불변식」). 아웃박스 행을 넣는 것은 ①이다 — 커밋된 글의 알림이 유실되지 않으려면 그 행이 글과 함께 커밋돼야
 * 한다. 발송만 ②다. 통째로 밀면 커밋 직후 죽는 창에서 알림이 사라져 아웃박스를 두는 이유 자체가 없어진다.
 *
 * <p><b>이 이벤트를 알림이 듣지는 않을 것이다.</b> NT-06 의 알림 종류는 셋이고 (내 모집글의 댓글 · 내 댓글의 답글 · 채팅방 새 메시지) 모집글 작성이
 * 없다. 위 규약은 이 이벤트만의 것이 아니라 <b>이 저장소의 도메인 이벤트 전부</b>에 대한 것이고, 이것이 첫 번째라 여기 적혔다.
 */
public record CompanionPostOpened(Long postId, Long hostId) {}

-- Identity / Auth 대역(V10~V19). V10 · V11(STAR-40) · V12 · V13(STAR-47) 이 쓰였고 다음이 V14 다.
-- 고르기 전에 열려 있는 PR 도 확인했다 — 신고가 V34, 그 밖에 대역 안을 쓰는 것은 없다.
--
-- STAR-40 이 컬럼을 intro 로 만들었는데 API 설계 2-2 가 응답 필드를 bio 로 못박았다
-- ("한줄소개 필드명은 bio 다. 도메인 Profile 의 「한줄소개」가 이것이다").
-- API 컨벤션이 "도메인 식별자와 API 필드명을 일치시킨다" 이므로 컬럼을 API 쪽으로 맞춘다.
--
-- 반대 방향(응답을 intro 로)이 아닌 이유 — 응답 필드명은 클라이언트 계약이고 화면 계약과
-- types.ts 가 이미 bio 를 쓴다. 컬럼명은 우리만 본다.
--
-- V11 의 시드는 intro 로 INSERT 하는데, 번호순으로 V11 이 먼저 적용되므로 그대로 동작한다.
-- 적용된 마이그레이션은 고치지 않는다 — 체크섬이 깨진다.
ALTER TABLE user
    RENAME COLUMN intro TO bio;
